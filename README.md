# ImageFetcher

iOS app that fetches and displays remote photos (Albums → Photos → Full Image) using programmatic UIKit and VIPER-style modules.

## Setup

Open **`ImageFetcher.xcodeproj`** in Xcode and run. No CocoaPods or other dependency managers are required.

## Requirements

- Xcode 12+
- iOS 14.2+
